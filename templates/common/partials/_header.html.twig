{#
<nav class="navbar navbar-expand-lg navbar-dark bg-dark py-2 sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand me-5" href="{{  path('home_index') }}"><i class="far fa-comments me-3 fs-3"></i>ForumBooster</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li>
                    <a class="nav-link" aria-current="page"
                            {% if  app.user %}
                        href=""
                    {% else %}
                        href="{{ path('app_login')}}"

                            {% endif %}>Mon compte
                    </a>
                </li>
                {% if app.user %}
                {% else %}
                    <li>
                        <a class="nav-link" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
                    </li>
                {% endif %}

            </ul>
            <span class="navbar-text">
                {% if app.user %}
                    <span class="text-light me-3">Hello {{ app.user.firstName~' '~app.user.lastName }} !</span>
                    <a class="btn btn-outline-warning" href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a class="btn btn-outline-warning" href="{{ path('app_login') }}">Se connecter</a>
                {% endif %}
            </span>
        </div>
    </div>
</nav>#}
<nav class="navbar navbar-expand-lg navbar-light bg-light d-flex justify-content-between py-3 px-5">
    <a class="navbar-brand d-flex align-items-center" href="{{  path('home_index') }}">
        <img class="img-fluid" src="images/logo.svg" alt="logo" />
        <div class="d-flex flex-column">
            <h1 class="logoText"><span class="logoText-green">La</span> <span class="logoText-pink">Nîmes</span><span class="logoText-green">'alerie</span></h1>
        </div>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
        <div class="navbar-toggler-icon">
            <span></span>
            <span></span>
            <span></span>
        </div>

    </button>
    <div class="offcanvas offcanvas-end" id="navbarToggler">
        <div class="offcanvas-header">
            <a class="btn" role="button" data-bs-dismiss="offcanvas" aria-label="Close"><i class="icon-close-black"></i></a>

        </div>
        <ul class="navbar-nav fw-bold fs-5 gap-5 justify-content-end">
            <li class="nav-item position-relative">
                {#TODO ACTIVEPAGE AVEC TWIG ET ROUTE#}
                <a class="nav-link activePage" href="">Catégories</a>
            </li>
            <li class="nav-item position-relative">
                <a class="nav-link" href="">Services</a>
            </li>
            <li class="nav-item position-relative">
                <a class="nav-link" href="">Contact</a>
            </li>
            <li>
                <a class="nav-link" aria-current="page"
                        {% if  app.user %}
                    href=""
                {% else %}
                    href="{{ path('app_login')}}"

                        {% endif %}>Mon compte
                </a>
            </li>
            {% if app.user %}
            {% else %}
                <li>
                    <a class="nav-link" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
                </li>
            {% endif %}
            <li>
                <span class="nav-link">
                {% if app.user %}
                    <span class="text-light me-3">Hello {{ app.user.firstName~' '~app.user.lastName }} !</span>
                    <a class="btn btn-outline-warning" href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a class="btn btn-outline-warning" href="{{ path('app_login') }}">Se connecter</a>
                {% endif %}
                </span>
            </li>

        </ul>

    </div>
</nav>
